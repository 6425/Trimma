{
  "name": "trimma-monorepo",
  "private": true,
  "packageManager": "npm@10.0.0",
  "workspaces": [
    "apps/*",
    "packages/*",
    "services/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=@trimma/web",
    "dev:restart": "npm run dev:restart --workspace=@trimma/web",
    "dev:all": "turbo run dev",
    "build": "turbo run build",
    "start": "node -e \"const cp = require('child_process'); cp.spawn('npm', ['run', 'start', '--workspace=@trimma/web'], { stdio: 'inherit', shell: true }); cp.spawn('npm', ['run', 'start:prod', '--workspace=@trimma/api-gateway'], { stdio: 'inherit', shell: true });\"",
    "lint": "turbo run lint",
    "clean": "turbo run clean"
  },
  "devDependencies": {
    "puppeteer": "^25.1.0",
    "turbo": "latest"
  },
  "dependencies": {
    "dotenv": "^17.4.2",
    "pg": "^8.21.0"
  }
}
