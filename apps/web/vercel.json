{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "buildCommand": "cd ../.. && TRIMMA_LISTING_UNCAP=id-cursor npx turbo run build --filter=@trimma/web... --force --no-cache",
  "installCommand": "npm install --prefix=../..",
  "git": {
    "deploymentEnabled": true
  }
}
